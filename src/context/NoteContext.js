import { createContext } from "react";

const noteContext=createContext(); //This is a syntax to create a new syntax.Context will hold all our states related to notes in order for us to access our notes wherever and whenver we want easily.

export default noteContext;